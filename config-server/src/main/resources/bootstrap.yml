spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:/C:/Users/mdz/workspace/edu/microservices/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true